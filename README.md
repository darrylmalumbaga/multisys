## multisys

```bash
# composer install
# cp .env.example .env
# change MAIL on .env with your mailtrap credentials
# set QUEUE_CONNECTION to database
# php artisan migrate
# php artisan db:seed
# php artisan passport:install
# php artisan test
# php artisan queue:work

Available API:
/api/register
/api/login
/api/order
